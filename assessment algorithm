1. Количество операций:
4*(N-1) - количество условий * количество обрабатываемых символов в файле.
+ 2*(N-1) - добавление новых символов в файл в худшем исходе.
*N - считывание символов пока не конец файла.
+ 11 - операций в main.
Итого: 6*(N-1)*N + 11 = 6N^2+5;
Сложность алгоритма: O(n^2);

2. По памяти:
N - размер файла в байтах
+ 1 - пустой файл, но проверки на пустоту нет, так что может быть и размер M байт
+ 160 - максимальное название двух файлов
166мб отводится для всей программы 
Итого: 166мб + Nб + Mб + 160б
